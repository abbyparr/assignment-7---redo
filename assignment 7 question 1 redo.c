def probability(x,y,n,z):
    n = input('input the dice configuration')
    z = input('input numeric value')
    
  
    for (x):
        n[0] * n[2] = x
        
    for (probability):
        y = x - z
        probability = y / x
        print('the probability is',probability)
        
    if (n != [3]):
        print('the input is not a dice configuration. input a dice configuartion')
        